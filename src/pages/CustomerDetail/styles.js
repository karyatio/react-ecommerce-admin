const styles = theme => ({
  bio: {
    padding: theme.spacing(2)
  },
  imgFluid: {
    maxWidth: "100%",
    height: "auto"
  },
  paper: {
    padding: theme.spacing(2)
  },
  container: {
    paddingTop: theme.spacing(4),
    paddingBottom: theme.spacing(4)
  }
});

export default styles;
